{
  "name": "00-setup-with-server",
  "version": "1.0.0",
  "description": "typescript setup",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "tsc -w",
    "serve": "lite-server",
    "start": "run-p watch serve"
  },
  "author": "Dalairac Diego",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.6.1",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "typescript": "^4.7.4"
  }
}
